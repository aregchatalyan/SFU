{
  "name": "sfu",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "server": "cross-env NODE_ENV=production node app.js",
    "server:dev": "cross-env NODE_ENV=development nodemon app.js",
    "server:install": "npm install",
    "client:install": "npm install --prefix view",
    "client:build": "npm run build --prefix view",
    "start": "npm run client:install && npm run client:build && npm run server:install && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "express": "^4.17.1",
    "httpolyglot": "^0.1.2",
    "mediasoup": "^3.8.2",
    "mediasoup-client": "^3.6.37",
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
